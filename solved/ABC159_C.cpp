#include <bits/stdc++.h>
#define rep(i,n) for(int i=0;i<(n);i++)
#define all(x) (x).begin(), (x).end()
using ll = long long;
using namespace std;

int main() {
	double L;
	cin >> L;
	double a = L/3;
	cout << fixed << setprecision(9);
	cout << a*a*a << endl;
}