version: '3.3'

services:
  cpp:
    build: .
    volumes:
      - ..:/cpp
    working_dir: /
    tty: true
    # to use gdb
    cap_add:
      - SYS_PTRACE
    security_opt:
      - seccomp:unconfined